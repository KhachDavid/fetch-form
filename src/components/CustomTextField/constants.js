// this file defines the types for the CustomTextField component

export const TEXTFIELD_TYPES = {
  TEXT: "text",
  PASSWORD: "password",
  EMAIL: "email",
};

// we only use one textfield variants
// upon needing more variants, we can add them here
export const TEXTFIELD_VARIANTS = {
  OUTLINED: "outlined",
}
