// This file defines the constants for the CustomSelect component

// Currently, we only use one select variant
// upon needing more variants, we can add them here
export const SELECT_VARIANTS = {
    STANDARD: "standard",
};